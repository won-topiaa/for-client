/**
 * 배치 인스톨러 — 기본 설정 / 기본 그룹 목록
 * ------------------------------------------------------------------
 * · 여기 값은 "공장 초기값"입니다. 앱을 처음 켜면 이 목록으로 시작하고,
 *   이후 앱 안에서 편집한 내용은 기기에 저장(storages)되어 다음 실행 때
 *   그대로 유지됩니다. (즉, 손님은 앱 안에서 수시로 목록을 바꿀 수 있음)
 * · "공장초기화"를 누르면 다시 이 목록으로 되돌아옵니다.
 *
 * 앱 식별 방법
 * ------------------------------------------------------------------
 * 각 앱은 "이름(name)"만 있으면 됩니다. 설치 시 이 이름으로 플레이스토어를
 * 검색합니다. 더 정확한 설치를 원하면 패키지명(pkg)을 같이 넣으세요.
 * 패키지명이 있으면 검색을 건너뛰고 해당 앱 상세페이지로 바로 이동합니다.
 *   예) { name: "연합뉴스", pkg: "com.yonhapnews.andrapp" }
 */
"use strict";

module.exports = {
  // ── 기본 그룹 목록 ────────────────────────────────────────────
  // 손님이 예시로 준 구성 + 테스트 3종을 기본값으로 넣어둠
  defaultGroups: [
    {
      name: "테스트 3종",
      apps: [
        { name: "연합뉴스" },
        { name: "조선일보" },
        { name: "공영쇼핑" },
      ],
    },
    {
      name: "그룹 1",
      apps: [
        { name: "연합뉴스" },
        { name: "공영쇼핑" },
      ],
    },
    {
      name: "그룹 2",
      apps: [
        { name: "공영쇼핑" },
        { name: "빗썸" },
      ],
    },
  ],

  // ── 동작 타이밍(ms) ───────────────────────────────────────────
  timing: {
    afterOpenStore: 2500,   // 플레이스토어 화면 뜬 뒤 대기
    afterSearch: 2200,      // 검색 결과 뜰 때까지 대기
    afterOpenDetail: 1800,  // 상세페이지 진입 대기
    clickPoll: 800,         // 버튼 상태 재확인 간격
    installTimeout: 300000, // 앱 1개 설치 최대 대기(5분). 초과 시 실패로 넘어감
    betweenApps: 1500,      // 다음 앱으로 넘어가기 전 여유
  },

  // ── 플레이스토어 버튼 텍스트(언어/버전 대응용) ────────────────
  // 기기 언어가 영어면 영문 라벨도 잡히도록 함께 넣어둠
  labels: {
    install: ["설치", "Install"],
    update:  ["업데이트", "Update"],
    open:    ["열기", "Open", "실행"],
    installed: ["제거", "Uninstall", "설치 완료"],
    cancel:  ["취소", "Cancel"],
    // "이 기기와 호환되지 않습니다" 등 설치 불가 신호
    incompatible: ["호환되지 않", "not compatible", "설치할 수 없", "지원되지 않는"],
  },

  storedPackage: "com.android.vending", // 구글 플레이스토어 패키지
};
