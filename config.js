/**
 * TikTok Lite 자동화 설정 파일
 * ------------------------------------------------------------------
 * 실기기마다 화면 해상도/UI 위치/버튼 텍스트가 다릅니다.
 * 코드를 건드리지 말고 이 파일의 값만 자기 폰에 맞게 수정하세요.
 *
 * 좌표는 "화면 비율(0.0 ~ 1.0)"로 지정합니다.
 *   x: 0 = 화면 왼쪽 끝, 1 = 오른쪽 끝
 *   y: 0 = 화면 위쪽 끝, 1 = 아래쪽 끝
 * 예) 화면 정중앙 = { x: 0.5, y: 0.5 }
 *
 * 좌표 찾는 법: AutoX.js 개발자 옵션에서 "포인터 위치" 표시를 켜고
 * 원하는 버튼을 눌러 픽셀값을 확인 → (픽셀 / 화면크기)로 비율 계산.
 */
module.exports = {
  // ── 앱 정보 ────────────────────────────────────────────────
  // TikTok Lite 패키지명. 기종/버전에 따라 둘 중 하나입니다.
  // 확인: 설정 > 앱 > TikTok Lite, 또는 아래 두 후보를 순서대로 시도.
  appName: "TikTok Lite",
  packageCandidates: [
    "com.ss.android.ugc.aweme.lite", // 글로벌 TikTok Lite
    "com.zhiliaoapp.musically.go",    // 일부 지역 TikTok Lite
  ],

  // ── 대기 시간(ms) ──────────────────────────────────────────
  timing: {
    afterLaunch: 6000,      // 앱 실행 후 로딩 대기
    afterPopupClose: 1200,  // 팝업 닫은 뒤 대기
    beforePointBtn: 15000,  // (4단계) 포인트 버튼 누르기 전 15초 대기
    afterAdWatch: 30000,    // (8단계) 광고 후 30초 대기
    shortWait: 1500,        // 화면 전환 등 짧은 대기
    loopIntervalMs: 20 * 60 * 1000, // (6단계) 20분마다 타이머 반복
  },

  // ── 버튼 텍스트(접근성 서비스로 우선 탐색) ─────────────────
  // 폰 언어에 맞게 수정하세요. 여러 후보를 배열로 넣으면 순서대로 찾습니다.
  texts: {
    receive: ["받기", "受け取る", "Collect", "Claim", "Receive"], // 받기
    watchAd: ["광고 보기", "광고보기", "動画を見る", "Watch", "Watch ad"], // 광고보기
    timer:   ["타이머", "Timer"],
    close:   ["닫기", "×", "✕", "X", "Close", "取消", "Skip", "건너뛰기"], // 팝업 닫기
  },

  // ── 좌표 폴백(텍스트로 못 찾을 때 이 비율 좌표를 누름) ─────
  // 반드시 자기 폰에서 실측해서 보정하세요. 아래는 대략적 초기값입니다.
  coords: {
    popupClose:  { x: 0.90, y: 0.12 }, // (2,5단계) 팝업 우측 상단 X
    heart:       { x: 0.93, y: 0.50 }, // (3단계) 우측 중간 하트
    pointButton: { x: 0.30, y: 0.95 }, // (4단계) 좌측 하단 두번째 포인트 버튼
    timer:       { x: 0.50, y: 0.85 }, // (6단계) 타이머 버튼
    topRightP:   { x: 0.93, y: 0.06 }, // (8단계) 우측 상단 p
    videoLike:   { x: 0.93, y: 0.55 }, // (9단계) 영상 좋아요(하트)
  },

  // 좌표를 못 찾았을 때 몇 번 재시도할지
  retry: {
    findTimeoutMs: 4000, // 텍스트 탐색 최대 대기
    maxAttempts: 3,
  },
};
