# API 키가 들어가는 실제 설정 파일은 커밋 금지 (config.example.json 만 커밋)
config.json

__pycache__/
*.pyc
.pytest_cache/
data/*.csv

# 회원 인증 SQLite DB(런타임 데이터) — 커밋 금지
data/*.db
data/*.db-shm
data/*.db-wal
*.db

# 시크릿 파일은 어떤 형태든 커밋 금지
*_secret.*
*.secret
